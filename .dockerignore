# Keep helper and local registry storage out of build contexts.
pb/docker/helper
pb/docker/helper/**
pb/docker/registry/data
pb/docker/registry/data/**

# VCS metadata is not required during image builds.
.git
