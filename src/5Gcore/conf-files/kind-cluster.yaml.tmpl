kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  podSubnet: ${KIND_POD_SUBNET}
  serviceSubnet: ${KIND_SERVICE_SUBNET}
  apiServerAddress: ${KIND_API_SERVER_ADDRESS}
${KIND_API_SERVER_PORT_BLOCK}${KIND_DISABLE_DEFAULT_CNI_BLOCK}containerdConfigPatches:
- |-
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."${KIND_REGISTRY_MIRROR_ENDPOINT}"]
    endpoint = ["${KIND_REGISTRY_MIRROR_URL}"]
  [plugins."io.containerd.grpc.v1.cri".registry.configs."${KIND_REGISTRY_ENDPOINT}".tls]
    ca_file = "/etc/containerd/certs.d/${KIND_REGISTRY_ENDPOINT}/ca.crt"
  [plugins."io.containerd.grpc.v1.cri".registry.configs."${KIND_REGISTRY_ENDPOINT}".auth]
    username = "${REGISTRY_USER}"
    password = "${REGISTRY_PASS}"
kubeadmConfigPatches:
${KIND_ETCD_UNAUTHENTICATED_KUBEADM_CONFIG_PATCH_BLOCK}- |-
  kind: KubeletConfiguration
  allowedUnsafeSysctls:
    - "net.ipv4.ip_forward"
nodes:
- role: control-plane
${KIND_CONTROL_PLANE_EXTRA_MOUNTS_BLOCK}${KIND_ETCD_UNAUTHENTICATED_PORT_MAPPING_BLOCK}${KIND_WORKER_NODES_BLOCK}
