#
# Software Name : towards5gs-helm
# SPDX-FileCopyrightText: Copyright (c) 2021 Orange
# SPDX-License-Identifier: Apache-2.0
#
# This software is distributed under the Apache License 2.0,
# the text of which is available at https://github.com/Orange-OpenSource/towards5gs-helm/blob/main/LICENSE
# or see the "LICENSE" file for more details.
#
# Author: Abderaouf KHICHANE, Ilhem FAJJARI, Ayoub BOUSSELMI, Ian Chen
# Software description: An open-source project providing Helm charts to deploy 5G components (Core + RAN) on top of Kubernetes
#
{{- with .Values.pcf }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "free5gc-pcf.fullname" $ }}-configmap
  labels:
    app: {{ $.Values.global.projectName }}
data:
  pcfcfg.yaml: |
    info:
      version: 1.0.2
      description: PCF initial local configuration

    configuration:
      serviceList:
        {{- .configuration.serviceList | nindent 8 }}

      sbi:
        scheme: {{ $.Values.global.sbi.scheme }}
        registerIPv4: {{ include "free5gc-pcf.fullname" $ }}-service # IP used to register to NRF
        bindingIPv4: 0.0.0.0  # IP used to bind the service
        port: {{ .service.port }}
        tls:
          key: ./cert/pcf.key
          pem: ./cert/pcf.pem
{{- if $.Values.pcf.configuration.metrics.enable }}
      metrics: 
        enable: true
        scheme: {{ $.Values.pcf.configuration.metrics.scheme }}
        bindingIPv4: 0.0.0.0  # IP used to bind the service
        port: {{ $.Values.pcf.configuration.metrics.port }}
        namespace: {{ $.Values.pcf.configuration.metrics.namespace }}
        tls:
          key: ./cert/pcf.key
          pem: ./cert/pcf.pem
{{- end }}

      mongodb:       # the mongodb connected by this PCF
        name: free5gc                  # name of the mongodb
        url: MONGODB_URL_PLACEHOLDER # a valid URL of the mongodb; populated at runtime from Secret

      nrfUri: {{ $.Values.global.sbi.scheme }}://{{ $.Values.global.nrf.service.name }}:{{ $.Values.global.nrf.service.port }}
      nrfCertPem: {{ ternary "oauth/nrf.pem" "cert/nrf.pem" $.Values.global.oauth.samba.enabled }}

      {{- .configuration.configuration | nindent 6 }}

    logger:
      {{- toYaml .configuration.logger | nindent 6 }}


  mongodb-uri-wrapper.sh: |
    #!/bin/sh
    set -eu

    # Build the authenticated MongoDB URI at runtime from a Kubernetes Secret.
    # free5GC requires the MongoDB URL inside its YAML config, so this wrapper
    # writes a temporary config file and keeps Secret data out of ConfigMaps.
    config_src="$1"
    config_dst="$2"
    shift 2

    : "${MONGODB_URL:?MONGODB_URL must be set from the MongoDB app Secret}"
    mkdir -p "$(dirname "$config_dst")"
    cp "$config_src" "$config_dst"

    escaped_uri=$(printf '%s' "$MONGODB_URL" | sed 's/[\/&]/\\&/g')
    sed -i "s/MONGODB_URL_PLACEHOLDER/${escaped_uri}/g" "$config_dst"

    exec "$@"

{{- end }}
