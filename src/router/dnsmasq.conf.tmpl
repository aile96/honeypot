# Ascolta solo su eth0
bind-interfaces
except-interface=lo
interface=eth0

# DNS forwarding/cache
no-resolv
server=${DNS_DOCKER}
cache-size=1000
domain-needed
bogus-priv

# Log su stdout
log-facility=-
